tags:
  - Contacts
summary: Create contact
operationId: createContact
description: 'Create a contact with payload'
security:
  - bearerAuth: []
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - phoneNumber
        properties:
          name:
            description: "Contact's name"
            type: string
            example: 'Joi'
          phoneNumber:
            description: "Contact's phone number"
            type: string
            example: '1234567890'
          email:
            description: "Contact's email"
            type: string
            example: 'keraberas007@ukr.net'
          isFavourite:
            description: "Whether the contact is a favourite. Should be true or false"
            type: boolean
            example: false
          contactType:
            description: "Contact's type of phone number"
            type: string
            enum: ['work', 'home', 'personal']
            example: personal
          photo:
            description: "Link to contact's photo"
            type: string
            format: binary
responses:
  '201':
    description: Creates a contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully created a contact!
            data:
              type: object
              $ref: '../../components/schemas/contact.yaml'
  '404':
    $ref: '../../components/responses/404.yaml'
  '401':
    $ref: '../../components/responses/401.yaml'
  '500':
    $ref: '../../components/responses/500.yaml'
